package Java.enums;

public enum GameState {
    // Name-constants to represent the various states of the game
    PLAYING(0), TIE(1), CROSS_WON(2), NOUGHT_WON(3);

    GameState(int value) {

    }
}
